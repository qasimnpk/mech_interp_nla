# legacy/ — earlier sentence-level protocol

Superseded by the atomic-claim protocol used for every reported result.
Kept for provenance only: labels (`02_atoms_7b.jsonl`, 402 sentence claims over 25 documents), their deletions and
scores (`04_atomic_*`), the blind review sheet, the old paraphrase guide, and the statistics scripts that read them.

Nothing in the write-up comes from these files. The current labels are `../tasks/02_atoms_*_doc*.jsonl` and the current
scores `../04_scores_7b_b*.csv` and `../04_06_scores_27b_full.csv`.
